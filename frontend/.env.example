# Base URL of the FastAPI backend. Never put secret keys in frontend env vars —
# the backend proxies all external providers (see backend/.env.example).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
